# Uncomment the next line to define a global platform for your project
platform :ios, '15.0'

target 'iOS-template-uikit' do
  use_frameworks!
  

  # Pods for iOS-template-uikit
  pod 'WolmoCore'

end
